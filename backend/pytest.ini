[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = test_*.py
testpaths = tests accounts channels_app common groups_app media_app messaging notifications realtime roles scheduling
addopts = --strict-markers -ra
